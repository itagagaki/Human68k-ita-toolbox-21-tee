


TEE(1)         Human68k ITA TOOLBOX User's Manual          TEE(1)



名称
     tee - パイプの T 字形ジョイント

形式
     tee [ -aiZ ] [ -- ] [ file ] ...

解説
     tee は，標準入力を標準出力に転送し，同じデータを file にも書
     き込みます．file は複数指定することができます．

     入力が cooked モードのキャラクタ・デバイス（CON など）である
     場合，^D (control-D) かまたは ^Z を読み取ると，それを EOF コー
     ドと見なして入力を打ち切ります．

     tee に指定できるオプションを以下に示します．

     -a   file が既存ファイルである場合に，出力を file の末尾に追
          加します．

     -i   ^C で中断しません．ただしバージョン2.0未満の Human68k
          ではこのオプションは無効です．

     -Z   いかなる入力からも，^Z コードを EOF と見なして入力を打
          ち切ります．

     これらのオプションは -a -i のように単独に記述することもでき
     ますし，-ai のように連結して指定することもできます．

     引数‘--’は，次の引数を file であると解釈させます．引数‘-’
     は標準出力を意味し，file として解釈されます．

関連事項
     cat(1)

診断
     何らかのエラーがあったときにはエラー・メッセージが標準エラー
     出力に出力されます．

     終了ステータスとしては，引数の記述に間違いがあった場合には 1，
     ファイルがオープンできなかった場合には 2，入出力エラーやメモ
     リ不足など，システムの問題により正常に実行できなかった場合に
     は 3，すべてが正常に終了したならば 0 が返ります．^C などによっ
     て中断したときの終了ステータスは OS の仕様によります．

不具合
     cooked モードのキャラクタ・デバイスからは，^D や ^Z を EOF
     としてではなくデータとして入力することはできません．

     CON からは行単位で入力されますので，^D あるいは ^Z の後にも
     改行が必要です．

     file が‘-’である場合は標準出力に出力しますが，出力は一定の
     ブロック単位で行われますから，あまり意味がありません．

バージョン
     1.0

制作・著作
     Copyright(C)1991,93 by 板垣 史彦 (Itagaki Fumihiko)



1.0                 Last change: 19 Feb 1993     Itagaki Fumihiko



